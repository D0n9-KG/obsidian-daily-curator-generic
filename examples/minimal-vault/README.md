# Minimal example vault

This tiny vault demonstrates the intended input style.

Try:

```bash
python ../../scripts/bootstrap_vault.py --vault . --apply
python ../../scripts/scan_vault.py --vault . --date 2026-01-02
```

Then ask your agent to curate `2026-01-02`.
