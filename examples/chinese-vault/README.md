# 中文示例 vault

这个示例展示中文随时记的低摩擦输入方式。

可以先运行：

```bash
python ../../scripts/bootstrap_vault.py --vault . --apply
python ../../scripts/scan_vault.py --vault . --date 2026-01-02
```

然后让 agent 使用此 skill 整理 `2026-01-02`。
